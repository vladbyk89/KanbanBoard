profileBtn.addEventListener(
  "click",
  () => (profileWindow.style.display = "flex")
);

backToMain.addEventListener(
  "click",
  () => (profileWindow.style.display = "none")
);

createBoardBtn.addEventListener('click', () => console.log('12'))

